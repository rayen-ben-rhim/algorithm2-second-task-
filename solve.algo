ALGORITHM iterative_for
VAR
   one : INTEGER := 1;
   two : INTEGER := 2;
   three : INTEGER := 3;
   four : INTEGER := 4;
   fife : INTEGER := 5;
   six : INTEGER := 6;
   seven : INTEGER := 7;
   eight : INTEGER := 8;
   nine : INTEGER := 9;
   /**/
    plus : CHAR := +;
    mines : CHAR := -;
    division : CHAR := ÷;
    multiplication : CHAR := *;
    /**/
    sentence : STRING
    result : FLOAT
  
BEGIN
  read(sentence)
  write(result)
END